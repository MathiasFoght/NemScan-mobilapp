import {StyleSheet} from "react-native";

export default StyleSheet.create({
    center: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    loadingText: {
        marginTop: 10,
        color: '#102570',
    },
    errorText: {
        color: 'red',
    },
});
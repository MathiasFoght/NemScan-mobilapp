import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: { flex: 1, backgroundColor: '#F9FAFB' },
    content: { padding: 20, paddingTop: 0, overflow: 'hidden' },
    cardsRow: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        marginTop: 16,
    }
});
